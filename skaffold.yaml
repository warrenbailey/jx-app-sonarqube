apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/deanesmith/jx-app-sonarqube1:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/deanesmith/jx-app-sonarqube1:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-app-sonarqube1
        chartPath: charts/jx-app-sonarqube1
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/deanesmith/jx-app-sonarqube1"
          image.tag: "{{.DIGEST_HEX}}"
